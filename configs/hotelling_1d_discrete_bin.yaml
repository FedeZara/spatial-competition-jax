# 1D Hotelling with discrete actions + bin-based observations
# Uses the environment's native grid observations (local_view)
# instead of Gaussian-blob encoding.
#
# Observation per agent:
#   own_position (1) + own_price (1) + local_view (3×(R+1)) = 2 + 3×11 = 35

_parent: hotelling_1d_discrete.yaml

# Use bin-based observations from the environment
obs_type: bin
